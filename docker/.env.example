# Copy this to docker/.env and customize

# Qdrant REST API key (leave empty to disable authentication)
QDRANT_API_KEY=

# Neo4j credentials — used by both the container and the healthcheck
NEO4J_USER=neo4j
NEO4J_PASSWORD=password
